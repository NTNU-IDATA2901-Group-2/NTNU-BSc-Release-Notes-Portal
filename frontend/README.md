# Release Notes Portal — Frontend

Vue 3 + TypeScript + Vite SPA for the Release Notes Portal. See the [root README](../README.md) for setup, environment variables, and how the frontend is served in each runtime mode.
