meta {
  name: negative_compare_missing_param
  type: http
  seq: 8
}

get {
  url: {{baseurl}}/releasenotes/compare?releaseNoteOneId={{compare_rn_old}}
  body: none
  auth: inherit
}

params:query {
  releaseNoteOneId: {{compare_rn_old}}
}

headers {
  X-Test-Role: Admin
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
