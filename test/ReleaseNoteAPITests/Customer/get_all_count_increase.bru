meta {
  name: get_all_count_increase
  type: http
  seq: 5
}

get {
  url: {{baseurl}}/customers
  body: none
  auth: inherit
}

assert {
  res.body.length: gt {{customer_count}}
}

settings {
  encodeUrl: true
  timeout: 0
}
