meta {
  name: negative_no_auth_update
  type: http
  seq: 8
}

put {
  url: {{baseurl}}/customers/{{customer_create_customer}}
  body: json
  auth: inherit
}

body:json {
  {
    "name" : "ChangedTest"
  }
}

assert {
  res.status: eq 403
}

settings {
  encodeUrl: true
  timeout: 0
}
