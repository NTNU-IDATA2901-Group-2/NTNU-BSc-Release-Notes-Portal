meta {
  name: update_with_invalid_info
  type: http
  seq: 8
}

put {
  url: {{baseurl}}/customers/{{customer_create_customer}}
  body: json
  auth: inherit
}

body:json {
  {
  }
}

assert {
  res.status: eq 500
}

settings {
  encodeUrl: true
  timeout: 0
}
