meta {
  name: delete_feature_by_id
  type: http
  seq: 9
}

delete {
  url: {{baseurl}}/features/{{feature_create_feature}}
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
