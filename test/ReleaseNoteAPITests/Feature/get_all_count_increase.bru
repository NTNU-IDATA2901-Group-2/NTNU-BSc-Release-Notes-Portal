meta {
  name: get_all_count_increase
  type: http
  seq: 6
}

get {
  url: {{baseurl}}/features
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

assert {
  res.body.length: gt {{feature_count}}
}

settings {
  encodeUrl: true
  timeout: 0
}
