meta {
  name: get_all_count_increase
  type: http
  seq: 5
}

get {
  url: {{baseurl}}/features
  body: none
  auth: inherit
}

assert {
  res.body.length: gt {{feature_count}}
}

settings {
  encodeUrl: true
  timeout: 0
}
