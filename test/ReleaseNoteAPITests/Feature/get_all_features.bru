meta {
  name: get_all_features
  type: http
  seq: 1
}

get {
  url: {{baseurl}}/features
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

script:post-response {
  bru.setEnvVar("feature_count", res.body.length)
}

settings {
  encodeUrl: true
  timeout: 0
}
