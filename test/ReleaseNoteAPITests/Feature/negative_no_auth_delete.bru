meta {
  name: negative_no_auth_delete
  type: http
  seq: 11
}

delete {
  url: {{baseurl}}/features/{{feature_create_feature}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 403
}

settings {
  encodeUrl: true
  timeout: 0
}
