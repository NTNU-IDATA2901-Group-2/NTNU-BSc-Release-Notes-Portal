meta {
  name: update_with_invalid_info
  type: http
  seq: 10
}

put {
  url: {{baseurl}}/features/{{feature_create_feature}}
  body: json
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

body:json {
  {
  }
}

assert {
  res.status: eq 500
}

settings {
  encodeUrl: true
  timeout: 0
}
