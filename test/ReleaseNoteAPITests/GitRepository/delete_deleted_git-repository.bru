meta {
  name: delete_deleted_git-repository
  type: http
  seq: 10
}

delete {
  url: {{baseurl}}/git-repositories?id={{createdGitRepositoryId}}
  body: none
  auth: inherit
}

params:query {
  id: {{createdGitRepositoryId}}
}

headers {
  X-Test-Role: Admin
}

assert {
  res.status: eq 404
}

settings {
  encodeUrl: true
  timeout: 0
}
