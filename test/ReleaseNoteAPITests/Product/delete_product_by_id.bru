meta {
  name: delete_product_by_id
  type: http
  seq: 9
}

delete {
  url: {{baseurl}}/products/{{product_create_product}}
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
