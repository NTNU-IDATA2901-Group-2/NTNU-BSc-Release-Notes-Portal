meta {
  name: update_with_invalid_info
  type: http
  seq: 8
}

put {
  url: {{baseurl}}/products/{{product_create_product}}
  body: json
  auth: inherit
}

body:json {
  {
  }
}

assert {
  res.status: eq 500
}

settings {
  encodeUrl: true
  timeout: 0
}
