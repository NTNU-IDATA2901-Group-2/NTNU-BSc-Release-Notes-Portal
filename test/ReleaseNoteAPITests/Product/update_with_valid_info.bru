meta {
  name: update_with_valid_info
  type: http
  seq: 7
}

put {
  url: {{baseurl}}/products/{{product_create_product}}
  body: json
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

body:json {
  {
    "name" : "ChangedTest"
  }
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
