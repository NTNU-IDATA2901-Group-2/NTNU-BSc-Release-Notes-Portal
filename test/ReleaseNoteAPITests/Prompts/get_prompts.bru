meta {
  name: get_prompts
  type: http
  seq: 1
}

get {
  url: {{baseurl}}/ai/prompts
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.length: eq 3
  : eq
}

settings {
  encodeUrl: true
  timeout: 0
}
