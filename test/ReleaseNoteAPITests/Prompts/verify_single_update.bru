meta {
  name: verify_single_update
  type: http
  seq: 7
}

get {
  url: {{baseurl}}/ai/prompts
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

assert {
  res.status: eq 200
  res.body.length: eq 2
  res.body[0].prompt: eq "test"
  res.body[1].prompt: eq "test"
}

settings {
  encodeUrl: true
  timeout: 0
}
