meta {
  name: get_all
  type: http
  seq: 6
}

get {
  url: {{baseurl}}/releasenotes
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.length: eq 2
}

settings {
  encodeUrl: true
  timeout: 0
}
