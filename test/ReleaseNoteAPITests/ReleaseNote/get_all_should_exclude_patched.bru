meta {
  name: get_all_should_exclude_patched
  type: http
  seq: 30
}

get {
  url: {{baseurl}}/releasenotes
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

assert {
  res.status: eq 200
  res.body.length: eq 0
}

settings {
  encodeUrl: true
  timeout: 0
}
