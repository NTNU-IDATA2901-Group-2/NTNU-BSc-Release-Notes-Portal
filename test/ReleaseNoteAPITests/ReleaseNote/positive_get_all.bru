meta {
  name: positive_get_all
  type: http
  seq: 11
}

get {
  url: {{baseurl}}/releasenotes
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

assert {
  res.status: eq 200
  res.body.content.length: eq 2
}

settings {
  encodeUrl: true
  timeout: 0
}
