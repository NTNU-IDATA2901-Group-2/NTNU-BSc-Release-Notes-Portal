meta {
  name: positive_get_all_filter_published
  type: http
  seq: 13
}

get {
  url: {{baseurl}}/releasenotes?published=true
  body: none
  auth: inherit
}

params:query {
  published: true
}

headers {
  X-Test-Role: Admin
}

assert {
  res.status: eq 200
  res.body.length: eq 1
  res.body[0].id: eq {{release_id_2}}
}

settings {
  encodeUrl: true
  timeout: 0
}
