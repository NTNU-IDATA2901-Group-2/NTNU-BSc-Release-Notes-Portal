meta {
  name: get_all_count_increase
  type: http
  seq: 5
}

get {
  url: {{baseurl}}/scopes
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

assert {
  res.body.length: gt {{scope_count}}
}

settings {
  encodeUrl: true
  timeout: 0
}
