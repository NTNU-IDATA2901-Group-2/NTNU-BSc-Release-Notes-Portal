meta {
  name: get_all_scopes
  type: http
  seq: 1
}

get {
  url: {{baseurl}}/scopes
  body: none
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

script:post-response {
  bru.setEnvVar("scope_count", res.body.length)
}

settings {
  encodeUrl: true
  timeout: 0
}
