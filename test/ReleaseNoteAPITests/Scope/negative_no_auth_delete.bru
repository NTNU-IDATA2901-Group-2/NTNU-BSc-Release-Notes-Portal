meta {
  name: negative_no_auth_delete
  type: http
  seq: 12
}

delete {
  url: {{baseurl}}/scopes/{{scope_create_scope}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 403
}

settings {
  encodeUrl: true
  timeout: 0
}
