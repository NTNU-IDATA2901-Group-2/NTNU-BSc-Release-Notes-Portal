meta {
  name: post_changenote_with_scope_in_use
  type: http
  seq: 16
}

post {
  url: {{baseurl}}/changenotes
  body: json
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

body:json {
  {
    "title": "Change note referencing in-use scope",
    "reference": "REF-IN-USE-SCOPE",
    "description": "Blocks deletion of the referenced scope",
    "scopeId": {{scope_in_use_id}}
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setEnvVar("scope_in_use_change_id", res.body)
}

settings {
  encodeUrl: true
  timeout: 0
}
