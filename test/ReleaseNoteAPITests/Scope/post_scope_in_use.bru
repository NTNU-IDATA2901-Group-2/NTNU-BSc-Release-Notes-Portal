meta {
  name: post_scope_in_use
  type: http
  seq: 15
}

post {
  url: {{baseurl}}/scopes
  body: json
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

body:json {
  {
    "name" : "inUseScope"
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setEnvVar("scope_in_use_id", res.body)
}

settings {
  encodeUrl: true
  timeout: 0
}
