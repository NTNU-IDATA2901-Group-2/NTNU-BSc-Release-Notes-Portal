meta {
  name: update_with_valid_info
  type: http
  seq: 8
}

put {
  url: {{baseurl}}/scopes/{{scope_create_scope}}
  body: json
  auth: inherit
}

headers {
  X-Test-Role: Admin
}

body:json {
  {
    "name" : "ChangedTest"
  }
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
